import { atom } from 'jotai';

// Atom to store the list of favorite artwork object IDs
export const favouritesAtom = atom([]);

// Atom to store the search history queries
export const searchHistoryAtom = atom([]);
